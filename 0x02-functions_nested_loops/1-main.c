#include "main.h"
#include <unistd.h>

/**
 * main -  prints the alphabet, in lowercase, followed by a new line
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	print_alphabet(void);
	return (0);
}
